import { ComingSoonCard } from "../_components/coming-soon-card";

export const metadata = { title: "Where They Come From — Business Brain" };

export default function WhereTheyComeFromTabPage() {
  return (
    <ComingSoonCard
      title="Where They Come From"
      phase="Phase 4.5"
      description="Booking source attribution. Direct visits, referrals via Pass the Torch, storefront browsers, and returning clients — so you know where to lean in."
    />
  );
}
